/**
 * MyScaling
 * @constructor
 */
class MyScaling extends MyKeyFrameAnimation {
    constructor(x, y, z) {
        //super(scene);

        this.initBuffers();
    };
    update() {};
    apply() {};
};