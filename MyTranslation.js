/**
 * MyRotation
 * @constructor
 */
class MyRotation extends MyKeyFrameAnimation {
    constructor(x, y, z) {
        //super(scene);

        this.initBuffers();
    };
    update() {};
    apply() {};
};