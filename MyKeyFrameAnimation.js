/**
 * MyKeyFrameAnimation
 * @constructor
 */
class MyKeyFrameAnimation extends MyAnimation {
    constructor(id, instant, tValues, rValues, sValues) { //values is a vector
        //super(scene);
        this.initBuffers();
    };
    update() {};
    apply() {};
};